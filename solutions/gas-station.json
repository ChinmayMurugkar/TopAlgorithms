{
  "problem": {
    "title": "Gas Station",
    "leetcode_id": null,
    "category": "String/Array/Matrix",
    "description": "There are n gas stations along a circular route, where the amount of gas at the ith station is gas[i].",
    "assumptions": [
      "If solution exists, it is guaranteed unique.",
      "Return -1 if no solution."
    ],
    "example": {
      "input": "gas = [1,2,3,4,5], cost = [3,4,5,1,2]",
      "output": "3",
      "explanation": "Start at station 3 (0-indexed): 4-1=3\u21923+5-2=6\u21926+1-3=4\u21924+2-4=2\u21922+3-5=0. Completes circuit."
    }
  },
  "solutions": [
    {
      "title": "Solution 1 \u2013 Greedy [\u2713 Interviewer Accepted]",
      "description": "Traverse all stations once, maintaining both a running current-tank balance and a global total balance. Whenever the current tank goes negative, the current starting station cannot be the answer, so reset the start to the next station and clear the tank. The key insight is that if the total gas is at least the total cost a solution is guaranteed to exist, and the candidate start found during the greedy scan is always that solution.",
      "code": "public int canCompleteCircuit(int[] gas, int[] cost) {\n    int totalGas = 0, totalCost = 0;\n    int start = 0, currentGas = 0;\n    \n    for (int i = 0; i < gas.length; i++) {\n        totalGas += gas[i];\n        totalCost += cost[i];\n        currentGas += gas[i] - cost[i];\n        \n        if (currentGas < 0) {\n            start = i + 1;\n            currentGas = 0;\n        }\n    }\n    \n    return totalGas >= totalCost ? start : -1;\n}",
      "code_python": "from typing import List\n\ndef can_complete_circuit(gas: List[int], cost: List[int]) -> int:\n    total_gas = total_cost = current_gas = 0\n    start = 0\n\n    for i in range(len(gas)):\n        total_gas += gas[i]\n        total_cost += cost[i]\n        current_gas += gas[i] - cost[i]\n\n        if current_gas < 0:\n            start = i + 1\n            current_gas = 0\n\n    return start if total_gas >= total_cost else -1",
      "language": "java",
      "complexity": {
        "time": "O(n)",
        "space": "O(1)"
      }
    }
  ],
  "variations": [],
  "navigation": {
    "previous": "two-sum.html",
    "next": "two-sum-iii.html"
  }
}