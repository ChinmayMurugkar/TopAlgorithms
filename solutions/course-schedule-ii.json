{
  "problem": {
    "title": "Course Schedule II",
    "leetcode_id": 210,
    "category": "Graph",
    "description": "There are a total of numCourses courses you have to take, labeled from 0 to numCourses - 1. You are given an array prerequisites where prerequisites[i] = [ai, bi] indicates you must take course bi before course ai. Return the ordering of courses you should take to finish all courses. If it is impossible to finish all courses, return an empty array.",
    "assumptions": [
      "Return one valid topological ordering.",
      "If a cycle exists (circular prerequisites), return [].",
      "There may be multiple valid answers."
    ],
    "example": {
      "input": "numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]]",
      "output": "[0,2,1,3]",
      "explanation": "Topological sort via Kahn's algorithm: track in-degree of each node; start with in-degree 0 nodes; repeatedly remove them and reduce neighbors' in-degrees. Cycle detected if result length < numCourses."
    }
  },
  "solutions": [
    {
      "title": "Solution 1 – BFS Topological Sort (Kahn's Algorithm)",
      "description": "Build an adjacency list and compute in-degrees. Add all nodes with in-degree 0 to a queue. Process each node: add it to the result and decrement the in-degree of its neighbors. If a neighbor's in-degree becomes 0, enqueue it. If the result length equals numCourses, a valid order exists; otherwise there is a cycle.",
      "code": "public int[] findOrder(int numCourses, int[][] prerequisites) {\n    List<List<Integer>> graph = new ArrayList<>();\n    int[] inDegree = new int[numCourses];\n    for (int i = 0; i < numCourses; i++) graph.add(new ArrayList<>());\n    for (int[] pre : prerequisites) { graph.get(pre[1]).add(pre[0]); inDegree[pre[0]]++; }\n    \n    Queue<Integer> queue = new LinkedList<>();\n    for (int i = 0; i < numCourses; i++) if (inDegree[i] == 0) queue.offer(i);\n    \n    int[] order = new int[numCourses];\n    int idx = 0;\n    while (!queue.isEmpty()) {\n        int course = queue.poll();\n        order[idx++] = course;\n        for (int next : graph.get(course)) if (--inDegree[next] == 0) queue.offer(next);\n    }\n    \n    return idx == numCourses ? order : new int[0];\n}",
      "code_python": "from typing import List\nfrom collections import deque\n\ndef find_order(num_courses: int, prerequisites: List[List[int]]) -> List[int]:\n    graph = [[] for _ in range(num_courses)]\n    in_degree = [0] * num_courses\n    for a, b in prerequisites:\n        graph[b].append(a)\n        in_degree[a] += 1\n    \n    queue = deque(i for i in range(num_courses) if in_degree[i] == 0)\n    order = []\n    while queue:\n        course = queue.popleft()\n        order.append(course)\n        for nxt in graph[course]:\n            in_degree[nxt] -= 1\n            if in_degree[nxt] == 0:\n                queue.append(nxt)\n    \n    return order if len(order) == num_courses else []",
      "language": "java",
      "complexity": {
        "time": "O(V + E)",
        "space": "O(V + E)"
      }
    }
  ],
  "variations": [],
  "navigation": {}
}
