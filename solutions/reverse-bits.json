{
  "problem": {
    "title": "Reverse Bits",
    "leetcode_id": null,
    "category": "String/Array/Matrix",
    "description": "Reverse bits of a given 32 bits unsigned integer.",
    "assumptions": [
      "Input is a 32-bit unsigned integer.",
      "Reverse all 32 bits (including leading zeros)."
    ],
    "example": {
      "input": "n = 00000010100101000001111010011100",
      "output": "964176192 (00111001011110000010100101000000)",
      "explanation": "Reverse all 32 bits. Shift result left and n right, OR the last bit of n each iteration."
    }
  },
  "solutions": [
    {
      "title": "Solution 1 – Bit Manipulation",
      "description": "Iterate 32 times: in each iteration, shift the result left by 1 to make room, then OR in the lowest bit of n, and shift n right by 1 to expose the next bit. After 32 steps all bits have been transferred in reverse order.",
      "code": "public int reverseBits(int n) {\n    int result = 0;\n    \n    for (int i = 0; i < 32; i++) {\n        result = (result << 1) | (n & 1);\n        n = n >> 1;\n    }\n    \n    return result;\n}",
      "code_python": "def reverseBits(n: int) -> int:\n    result = 0\n    for _ in range(32):\n        result = (result << 1) | (n & 1)\n        n >>= 1\n    return result",
      "language": "java",
      "complexity": {
        "time": "O(1)",
        "space": "O(1)"
      }
    }
  ],
  "variations": [],
  "navigation": {}
}